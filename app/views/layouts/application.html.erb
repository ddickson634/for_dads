<!DOCTYPE html>
<html>
  <head>
    <title>ForDads</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"> 

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      
<nav>
    <div class="nav-wrapper teal accent-4">
 
      <ul id="nav-mobile" class="right">
      
       <% if user_signed_in? %>
       
        <li><a href="/posts/new" class="waves-effect waves-light btn black-text">MAKE A POST</a></li>
        
        <li><%= link_to "LOG OUT", destroy_user_session_path, method: :delete, class: "waves-effect waves-light btn teal-text" %></li>
        
      <% end %>
      
       <% if !user_signed_in? %>
       
        <li><a href="/users/sign_in" class="waves-effect waves-light btn teal-text pulse">LOG IN</a></li>
        
        <% end %>
      </ul>
    </div>
  </nav>
<nav>
    <div class="nav-wrapper grey">
     
      <ul id="nav-mobile" class="left">
      
       <% if user_signed_in? %>
       
        <li><a><%= link_to "EDIT PROFILE", edit_user_registration_path %></a></li>
        
        <% end %>
        
        <li><%= link_to "ABOUT", about_path %></li>
        
        
      
      </ul>
    </div>
  </nav>
      
       <div>
           <div>
              <% if current_page? (root_path) %>
               <h5><strong>POST FEED</strong></h5>
               <% else %>
                   <%= link_to "Back to Post Feed", root_path %>
                <% end %>
           </div>
           
           <% flash.each do |name, msg| %>
           <%= content_tag(:div, msg, class: "alert") %>
           <% end %>
       </div>
       <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
